<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <title>Camping Searcher</title>
        <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/css/bootstrap.min.css" integrity="sha384-Vkoo8x4CGsO3+Hhxv8T/Q5PaXtkKtu6ug5TOeNV6gBiFeWPGFN9MuhOf23Q9Ifjh" crossorigin="anonymous">
        <link rel="stylesheet" href="/stylesheets/main.css">
        <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.3.1/css/all.css" integrity="sha384-mzrmE5qonljUremFsqc01SB46JvROS7bZs3IO2EmfFsd15uHvIt+Y8vEf7N7fWAU" crossorigin="anonymous">
    </head>
    <body>
        <!-- ===== NAVBAR ===== -->
        <nav class="navbar navbar-expand-sm navbar-dark bg-dark mb-3">
            <a class="navbar-brand" href="/">Camping Searcher</a>
            <!-- <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav"
                aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button> -->
        
            <!-- The div below is commented out to allow users to see camps & login/logout stuff when in mobile sizes. Something is not currently working correctly and this is my solution to that for now -->
            <!-- <div class="collapse navbar-collapse" id="navbarNav"> -->
                <div class="navbar-nav">
        
                    <!-- My addition for fun if the video doesn't add more menu shortcuts or a dropdown -->
                    <a class="nav-item nav-link text-light" href="/campgrounds">Home</a>
        
                </div>
                <ul class="navbar-nav navbar-right ml-auto">
                    <!-- The if() statement below lets us run a JS function to determine if the user is logged in and display routing for login, sign up, logout appropriately as well as to show the user's name when logged in. -->
                    <% if(!currentUser) { %>
                    <li class="nav-item <%= typeof page !== 'undefined' && page === 'login' ? 'active' : '' %>">
                        <a class="nav-link" href="/login">Login</a>
                    </li>
                    <li class="nav-item <%= typeof page !== 'undefined' && page === 'register' ? 'active' : '' %>">
                        <a class="nav-link" href="/register">Sign Up</a>
                    </li>
                    <% } else { %>
                    <li class="nav-link"><a href="#">Signed in: <%= currentUser.username %></a></li>
                    <li class="nav-item">
                        <a class="nav-link text-center" href="/logout">Logout</a>
                    </li>
                    <% } %>
                </ul>
            <!-- </div> -->
        </nav>

        
        <div class="container">
            <!-- need to have a length to the array specified because a blank string for an error is inherently truthy and would not work properly -->
            <% if(error && error.length > 0) { %>
                <div class="alert alert-danger" role="alert">
                    <%= error %>
                </div>
            <% } %>
            <% if(success && success.length > 0) { %>
                <div class="alert alert-success" role="alert">
                    <%= success %>
                </div>
            <% } %>
        </div>
    
